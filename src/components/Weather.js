import React from 'react';
import { Link } from 'react-router-dom';

const Weather = () => {
    return (
        <div>
            <h5>Welcome to Weather page</h5>
            <Link to='/'>return</Link>
        </div>
    );
};

export default Weather;